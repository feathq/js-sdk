// Kept in lockstep with package.json. Not imported from the manifest
// because that requires JSON import assertions every consuming bundler
// would have to opt into.
export const SDK_VERSION = "0.3.0";
